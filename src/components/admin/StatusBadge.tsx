"use client"

/**
 * O mapa de estados vive no sistema de componentes (`ui/Badge`) para que
 * "suspenso" tenha a mesma cor no admin e na aplicação. Reexportado aqui
 * apenas para preservar o caminho de import usado pelas páginas.
 */
export { StatusBadge } from "@/components/ui"
