import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Criar conta",
  description: "Crie sua conta gratuita na Matriz Aprova e comece a se preparar para sua aprovação em concursos públicos.",
}

export default function CadastroLayout({ children }: { children: React.ReactNode }) {
  return children
}
